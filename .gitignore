# Python/Django
__pycache__/
*.pyc
venv/
env/
.env
staticfiles/
# NOTE: backend/db.sqlite3 is intentionally TRACKED so a fresh clone has the
# seeded products/print-areas for review. Production uses PostgreSQL (env),
# so this file is never read there.

# Next.js
node_modules/
.next/
.env.local

# Media — ignore runtime output only. Product base images (product_views) are
# TRACKED so the catalog renders on a fresh clone.
backend/media/design_uploads/
backend/media/generated_previews/
backend/media/renders/

# Stray root-level fonts duplicate (frontend fonts live in frontend/public/fonts)
/fonts/

# OS/editor junk
.DS_Store
.vscode/db.sqlite3.backup-before-postgres-switch
# Backup files
backend/db.sqlite3.backup-before-postgres-switch
media_backup_before_postgres_switch/
